1、上电后进行工作电流测试；
2、获取ID 66A报文中零部件号、硬件版本号、软件版本号进行换算并进行比对是否一致；
3、进入诊断扩展层；
4、通过进行安全访问；
5、读取零件号、硬件版本号、ECU软件版本号、Bootloader版本号；并和标准值进行对比是否一致；
6、ECU ID号读取并记录，该号码为唯一码且不全为0，不全为F；
7、依次将ID 07FFFF01、07FFFF02、07FFFF03、07FFFF04写入左前、右前、左后、右后位置；
8、控制测试板发送4次左前位置胎压、胎温、漏气标志位；通过ID 589获取对应的数据和标志位对比是否一致；
9、同样方法进行右前、左后、右后进行测试；
10、测试完毕后将四个位置ID号写入FFFFFFFF。
11、产品断电，再次上电；
12、进入诊断默认层，读取四个位置ID号，确认为FFFFFFFF。
13、IG无效、CAN总线停止所有报文；3S后测试产品静态电流。


零部件号对应关系：
网络报文中零部件号无字符E，需要软件添加，数据值为十进制；
网络报文中硬件版本号无字符H，需要软件添加，数据值为十进制；
网络报文中主软件版本号、次软件版本号拼接起来为软件版本号，无对应字符S、C需要软件添加；数据值为十进制；
